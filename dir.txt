crypto-playground/
├── app/
│   ├── __init__.py         # Initializes the Flask application
│   ├── routes.py           # Defines the server routes (e.g., serving the main page)
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css   # Your main stylesheet
│   │   ├── js/
│   │   │   ├── main.js     # Main JS file to handle UI events (button clicks, etc.)
│   │   │   └── crypto.js   # Logic for calling WASM functions and handling crypto operations
│   │   └── wasm/
│   │       ├── aes.wasm    # The compiled WebAssembly file for AES
│   │       ├── aes.js      # The JS glue code for AES, generated by the compiler
│   │       └── ...         # Other .wasm and .js files for DES, RSA, etc.
│   └── templates/
│       └── index.html      # The single HTML page for your application
│
├── crypto_src/             # <-- Put your original C++/Java source code here
│   ├── AES/
│   │   └── aes.cpp
│   ├── DES/
│   │   └── des.cpp
│   └── RSA/
│       └── rsa.cpp
│
├── tests/                  # Directory for your project's tests
│   ├── test_crypto.py      # Example: Unit tests for your backend logic (if any)
│   └── ...
│
├── .gitignore              # To ignore files like virtual environments, caches, etc.
├── build_wasm.sh           # A shell script to automate compiling your C++ to WASM
├── requirements.txt        # Lists Python dependencies (e.g., Flask)
├── run.py                  # The entry point to start the Flask server
└── README.md               # **Crucial for interviews**: Explains the project, setup, and how to use it